\begin{table*}[tbp]\centering\small
\setlength{\tabcolsep}{6pt}
\caption{\textbf{The Online Downstream Test: a NaN-intolerant model forces imputation -- which one survives deployment?} The downstream forecaster is a \emph{ridge} regression, which (unlike the NaN-native GBMs of Table~\ref{tab:gbm_downstream}) \emph{cannot accept a single missing value}: imputation is MANDATORY, and the only choice is the imputer. A ridge predicts the \emph{contemporaneous} value of a held-out panel target (the column best explained by the cross-section, chosen on \emph{train} only) purely from the OTHER columns at the same time -- there is \emph{no} target-lag anchor, so the score is driven entirely by how well the masked cross-section is reconstructed. 50\% of feature cells are missing (60\% contiguous blocks $+$ MCAR); the target is never imputed. Strict temporal 70/30 split with the feature scaler fit on \emph{train} rows only (no test leakage); mean over 3 missingness seeds. \textbf{(Utility, the forced choice.)} ZeroFill (column-mean) is the lazy do-nothing-equivalent the practitioner is forced into; it carries no cross-sectional information. The \emph{Oracle} (clean, un-masked features) is the unattainable upper bound, and Oracle $\gg$ ZeroFill on every panel (the headroom a good imputer can recover -- this is what makes the task informative). Among DEPLOYABLE methods (\textbf{bold} $=$ best Live $R^2$), \cafe{}'s point-in-time cross-sectional fill recovers most of that headroom and beats both ZeroFill and LOCF on all four panels. \textbf{(Honesty, reported vs live.)} The standard (batch) protocol imputes the WHOLE series once and reads test features from that fill (\emph{Reported}); deployment is online, so each test row is re-imputed STRICTLY point-in-time on the prefix up to that row (\emph{Live}, expanding walk-forward). The same trained model is scored both ways, so $\Delta = $Reported$-$Live isolates the imputation look-ahead. The non-causal imputers (LinearInterp, SoftImpute) post strong \emph{Reported} $R^2$ that \emph{collapses} Live ($\Delta > 0$): their batch advantage is pure look-ahead, the imputation analogue of the leak in Bryzgalova et al., \emph{Missing Financial Data} (RFS 2025). \cafe{} (and the other causal methods) have Reported $\equiv$ Live, $\Delta = 0$ \emph{by truncation invariance} (verified via \texttt{assert\_causal}). \emph{Online evaluation is the deployment-correct one: it is the only protocol that does not reward look-ahead.} \textbf{Verdict:} across 4 panels \cafe{} has the best Live $R^2$ among deployable (causal) methods in 4 (all), gaining 0.202 Live $R^2$ on average over the forced ZeroFill and gaining 0.267 over LOCF -- a real online improvement with zero look-ahead. When the downstream model cannot tolerate NaN you must impute, and \cafe{} is the best deployable choice on structured panels.}
\label{tab:online_downstream}
\begin{tabular}{@{}lc ccc@{}}
\toprule
 & & \multicolumn{3}{c}{Forecast $R^2$} \\
\cmidrule(lr){3-5}
Imputer & Causal? & Rep. & Live$\uparrow$ & $\Delta\!\downarrow$ \\
\midrule
\multicolumn{5}{@{}l}{\emph{Beijing (PM2.5 panel)} (headroom 1.110)}\\
ZeroFill (forced) & \ding{51} & -0.110 & -0.110 & 0.000 \\
LOCF & \ding{51} & -0.141 & -0.141 & 0.000 \\
\cafe{} (ours) & \ding{51} & 0.499 & \textbf{0.499} & 0.000 \\
\emph{Oracle (clean)} & \ding{51} & 1.000 & 1.000 & 0.000 \\
LinearInterp & \ding{55} & -0.571 & -0.727 & 0.156 \\
SoftImpute & \ding{55} & 0.479 & 0.428 & 0.052 \\
\midrule
\multicolumn{5}{@{}l}{\emph{Traffic (PEMS occupancy)} (headroom 0.082)}\\
ZeroFill (forced) & \ding{51} & 0.869 & 0.869 & 0.000 \\
LOCF & \ding{51} & 0.795 & 0.795 & 0.000 \\
\cafe{} (ours) & \ding{51} & 0.922 & \textbf{0.922} & 0.000 \\
\emph{Oracle (clean)} & \ding{51} & 0.951 & 0.951 & 0.000 \\
LinearInterp & \ding{55} & 0.819 & 0.808 & 0.012 \\
SoftImpute & \ding{55} & 0.878 & 0.874 & 0.003 \\
\midrule
\multicolumn{5}{@{}l}{\emph{Solar (NREL plant power)} (headroom 0.038)}\\
ZeroFill (forced) & \ding{51} & 0.904 & 0.904 & 0.000 \\
LOCF & \ding{51} & 0.845 & 0.845 & 0.000 \\
\cafe{} (ours) & \ding{51} & 0.944 & \textbf{0.944} & 0.000 \\
\emph{Oracle (clean)} & \ding{51} & 0.942 & 0.942 & 0.000 \\
LinearInterp & \ding{55} & 0.805 & 0.779 & 0.026 \\
SoftImpute & \ding{55} & 0.915 & 0.913 & 0.002 \\
\midrule
\multicolumn{5}{@{}l}{\emph{Air Quality (multisensor)} (headroom 0.154)}\\
ZeroFill (forced) & \ding{51} & 0.833 & 0.833 & 0.000 \\
LOCF & \ding{51} & 0.737 & 0.737 & 0.000 \\
\cafe{} (ours) & \ding{51} & 0.940 & \textbf{0.940} & 0.000 \\
\emph{Oracle (clean)} & \ding{51} & 0.987 & 0.987 & 0.000 \\
LinearInterp & \ding{55} & 0.823 & 0.800 & 0.023 \\
SoftImpute & \ding{55} & 0.854 & 0.851 & 0.003 \\
\bottomrule
\end{tabular}
\end{table*}
